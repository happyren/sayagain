{
  "name": "sayagain-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "check": "biome check . && tsc -b && tsc -p packages/proxy/tsconfig.ui.json && tsc -p packages/sdk/tsconfig.test.json && tsc -p packages/proxy/tsconfig.test.json && tsc -p packages/lint/tsconfig.test.json && vitest run",
    "typecheck:tests": "tsc -p packages/sdk/tsconfig.test.json && tsc -p packages/proxy/tsconfig.test.json && tsc -p packages/lint/tsconfig.test.json",
    "pretest": "tsc -p packages/proxy/tsconfig.ui.json"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "packageManager": "pnpm@11.21.0",
  "version": "0.14.0"
}
